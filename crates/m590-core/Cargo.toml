[package]
name = "m590-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared types, config placeholders, and version for M590Bridge"

[dependencies]
sha2 = "0.10"
