# M590Bridge · 项目说明

## 要解决什么问题

罗技 M590 等支持双机切换的鼠标，只能切换键鼠连接，**不能**同步剪贴板或文件。  
本项目在两台电脑上运行桥接服务：复制内容通过局域网同步，切机后可直接粘贴。

## 目标平台

| 平台 | 首期 |
|------|------|
| Ubuntu / Linux | 是 |
| Windows 10 | 是 |
| Android | 否（暂缓） |
| macOS | 否（未排期） |

## 功能分期（产品）

| 阶段 | 内容 | 状态（2026-08-10） |
|------|------|-------------------|
| **MVP** | 配对 + 文本剪贴板双向 | **已完成** |
| **V2 · 图片** | 图片剪贴板双向（位图/PNG） | **已完成**（Word 等可粘贴） |
| **V2 · 文件** | 文件元数据 + 按需/发送时传输 + 进度 + 流式 | **基本可用**（路径流式 + SHA-256，软上限 8GiB；无文件夹/桌面粘贴/断点续传） |
| **V3** | mDNS、安装包/自启、性能 | **部分完成**：mDNS + Linux `.deb` + 用户登录自启；大文件流式已完成；Windows 安装/自启未做 |
| **以后** | Android | 暂缓 |

**已否决**：仅「收图落盘 + 文件剪贴板」桌面粘贴捷径（原 task-019A）。

## 技术约束

- 不依赖 M590 切机键事件；复制时推送（文件可粘贴/发送时拉）  
- 默认局域网 + 配对
- localhost Hub 控制 API 使用进程临时令牌，Tauri origin 使用受限 CORS
- 大文件磁盘流式传输（同连接分片 + SHA-256；非整包进内存）
- 文件接收临时目录限制安全 `transfer_id`、单文件/总预留量和目标卷可用空间；残留 `.part` 会在会话初始化时清理
- PNG 图片在解码前检查元数据，声明与实际像素面积均有上限
- Linux 登录自启由设置页显式启停，只写当前用户的 XDG autostart 入口，不使用 root 或系统级 service
- 当前图片 = **剪贴板位图**，不是通用文件传输

## 文档入口

| 文档 | 内容 |
|------|------|
| `docs/plans/current.md` | 当前计划（必读） |
| `docs/tasks/` | 任务；019 已 cancelled |
| `AGENTS.md` | Agent 规则 |
| `docs/domain/protocol-draft.md` | 协议 |

## 本地

`.agent/local-environment.md`（gitignore）。`cargo run -p m590-ui`

Linux 设置页可开启「登录时自动启动」；关闭会删除当前用户的 autostart 入口。卸载包不会跨用户清理该文件，卸载前应先关闭开关。
