# M590Bridge · 项目说明

## 要解决什么问题

罗技 M590 等支持双机切换的鼠标，只能切换键鼠连接，**不能**同步剪贴板或文件。  
本项目在两台电脑上运行桥接服务：复制内容通过局域网同步，切机后可直接粘贴。

## 目标平台

| 平台 | 首期 |
|------|------|
| Ubuntu / Linux | 是 |
| Windows 10 | 是 |
| Android | 否（暂缓） |
| macOS | 否（未排期） |

## 功能分期（产品）

1. **MVP**：配对 + 文本剪贴板双向同步  
2. **V2**：图片；文件元数据同步 + 粘贴/发送时传输；进度  
3. **V3**：mDNS 发现、托盘 UI、传输性能优化（追 LocalSend 量级）  
4. **以后再说**：Android 扩展端（当前明确不做）

## 关键约束

- M590 物理切机键 **无** 稳定第三方「切机事件」API；同步策略为 **复制时推送 / 粘贴时拉文件**，不依赖切机键
- 默认仅局域网；需配对
- 大文件不进剪贴板内存，走专用传输通道（V2+）

## 文档入口

| 文档 | 内容 |
|------|------|
| `AGENTS.md` | Agent 总规则 |
| `docs/plans/current.md` | 当前计划 |
| `docs/tasks/` | 任务记录 |
| `docs/ui-spec.md` | UI 规格（Figma/实现对照） |
| `docs/agent/workflow.md` | 工作流 |
| `docs/discovery/` | 摸底与开放问题 |

## 本地开发提示

本机路径、配对测试 IP、串口/防火墙备注等写在：

```text
.agent/local-environment.md
```

该文件已 gitignore，不要提交。
