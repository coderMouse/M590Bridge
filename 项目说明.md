# M590Bridge · 项目说明

## 要解决什么问题

罗技 M590 等支持双机切换的鼠标，只能切换键鼠连接，**不能**同步剪贴板或文件。  
本项目在两台电脑上运行桥接服务：复制内容通过局域网同步，切机后可直接粘贴。

## 目标平台

| 平台 | 首期 |
|------|------|
| Ubuntu / Linux | 是 |
| Windows 10 | 是 |
| Android | 否（暂缓） |
| macOS | 否（未排期） |

## 功能分期（产品）

| 阶段 | 内容 | 状态（2026-08-05） |
|------|------|-------------------|
| **MVP** | 配对 + 文本剪贴板双向 | **已完成**（实机） |
| **V2a** | 图片剪贴板双向（位图/PNG 线载） | **已完成**（实机）；可粘到 Word/画图等 |
| **V2b** | 图片落盘 + 文件剪贴板（桌面可粘贴） | **待做** → `task-019` |
| **V2c** | 通用文件元数据 + 按需传输 + 进度 | **待做** |
| **V3** | mDNS、安装包/自启、传输性能 | **待做** |
| **以后** | Android | 明确暂缓 |

## 技术约束

- M590 物理切机键 **无** 稳定第三方「切机事件」API；策略为 **复制时推送**（文件可「粘贴/发送时拉」）  
- 默认仅局域网；需配对  
- 大文件不进剪贴板内存，走专用传输（V2c+）  
- 当前图片同步是 **剪贴板位图**，不是桌面文件粘贴

## 文档入口

| 文档 | 内容 |
|------|------|
| `AGENTS.md` | Agent 总规则 |
| `docs/plans/current.md` | **当前计划（新会话必读）** |
| `docs/tasks/` | 任务记录；下一刀 `task-019` |
| `docs/domain/protocol-draft.md` | 协议草案 |
| `docs/ui-spec.md` | UI 规格 |
| `docs/agent/workflow.md` | 工作流 |
| `docs/discovery/` | 结构 / 命令 / 开放问题 |

## 本地开发提示

本机路径、测试 IP、防火墙备注写在：

```text
.agent/local-environment.md
```

该文件已 gitignore，不要提交。

```bash
cargo run -p m590-ui
```
