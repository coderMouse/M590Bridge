# M590Bridge · 项目说明

## 要解决什么问题

罗技 M590 等支持双机切换的鼠标，只能切换键鼠连接，**不能**同步剪贴板或文件。  
本项目在两台电脑上运行桥接服务：复制内容通过局域网同步，切机后可直接粘贴。

## 目标平台

| 平台 | 首期 |
|------|------|
| Ubuntu / Linux | 是 |
| Windows 10 | 是 |
| Android | 否（暂缓） |
| macOS | 否（未排期） |

## 功能分期（产品）

| 阶段 | 内容 | 状态（2026-08-18） |
|------|------|-------------------|
| **MVP** | 配对 + 文本剪贴板双向 | **已完成** |
| **V2 · 图片** | 图片剪贴板双向（位图/PNG） | **已完成**（Word 等可粘贴） |
| **V2 · 文件** | 文件元数据 + 按需/发送时传输 + 进度 + 流式 | **单文件按需粘贴、手动多文件/目录批次及 Windows OS 多文件/目录粘贴均已完成真机验收**；Linux FUSE tree 已实现并通过本地挂载 smoke，Nautilus 跨机与断点续传未完成 |
| **V3** | mDNS、安装包/自启、性能 | **首轮完成**：Linux 发布/自启与 Windows NSIS/HKCU 自启均已真机验收；安装包未签名 |
| **以后** | Android | 暂缓 |

**已否决**：仅「收图落盘 + 文件剪贴板」桌面粘贴捷径（原 task-019A）。

## 技术约束

- 不依赖 M590 切机键事件；复制时推送（文件可粘贴/发送时拉）  
- 默认局域网 + 配对
- localhost Hub 控制 API 使用进程临时令牌，Tauri origin 使用受限 CORS
- 大文件磁盘流式传输（同连接分片 + SHA-256；非整包进内存）
- 文件接收临时目录限制安全 `transfer_id`、单文件/总预留量和目标卷可用空间；残留 `.part` 会在会话初始化时清理
- PNG 图片在解码前检查元数据，声明与实际像素面积均有上限
- Linux 登录自启由正式/standalone 桌面端的设置页显式启停，只写当前用户的 XDG autostart 入口；依赖 Vite 的开发壳禁止开启
- Windows 登录自启只写当前用户 HKCU Run；NSIS 为当前用户安装并在卸载时清理该值；开发壳同样禁止开启
- Windows 单文件 OLE 虚拟剪贴板已接入跨机 `FileRequest`：Linux→Windows Explorer 已完成按需传输、系统进度、取消、剪贴板替换和空文件真机验收
- Linux GNOME Wayland 已验证 `x11-fallback` 发布的 `text/uri-list` 可被 Nautilus
  粘贴且内容一致（task-050）；该结论只解除 FUSE 原型的剪贴板入口风险，不代表 Linux 按需传输已实现
- Linux FUSE 单文件按需读取已接入现有网络 `FileRequest`、有界管道与 `FileCancel`
  （task-052）；GNOME Wayland Nautilus ↔ Windows 曾真机验证网络惰性读取、系统原生进度、
  内容和取消。task-058 已将 Linux Hub 改为非阻塞背压；后续日志定位到首个 256 KiB 块
  入管道后可能被 Wayland 剪贴板所有权同步读取阻塞，现已暂停活跃流期间的该读取。单文件/
  tree 各 24 MiB 本地挂载以及 Windows→Linux 几十 MiB MP4 单文件真机复测均已通过，
  含大文件批次和生命周期仍待最终验收。
- 多文件/目录批次（task-056）可通过原生多选、文件夹选择或多路径拖放触发；task-057
  进一步将剪贴板后端可见的多路径/目录 file_list 自动转换为同一批次。Hub 不跟随
  symlink，按清单顺序复用单文件流，整批成功后才发布到接收目录，并提供整体/当前条目
  进度与整批取消。手动入口已跨机验收；OS 文件管理器直接粘贴仍分别等待
  task-058 真机收口。task-058 已把 Linux 批次清单投影为只读 FUSE tree，并按文件访问
  串行触发网络请求；路径安全、取消/替换/断线清理和本地真实挂载 smoke 已通过，但不能
  代替 GNOME Wayland + Nautilus 跨机验收。
- Windows 多文件/目录接收（task-057）已实现一个 OLE 虚拟文件集合，并在 Explorer 请求
  各文件内容时沿用现有网络通道串行惰性传输；首轮真机发现只能粘贴一个文件且目录失败，
  现已按 Explorer 契约改为通配枚举 `CFSTR_FILECONTENTS`，无网络集合探针已通过；完整
  standalone 日志已确认失败样本只收到单文件 offer；现已修正发送端 file_list 只取第一项、
  Linux 多路径末尾 CR 残留及批次失败后错误单文件降级，多路径/目录会发布完整批次，Windows
  10 真机测试已通过；Linux FUSE 虚拟目录树已进入 task-058 真机验收阶段。
- 「自动同步剪贴板」控制本机是否监控并向对端发送；接收对端内容不依赖该开关。单向复制只需发送端开启，双向复制需两端都开启
- 当前图片 = **剪贴板位图**，不是通用文件传输

## 文档入口

| 文档 | 内容 |
|------|------|
| `docs/plans/current.md` | 当前计划（必读） |
| `docs/tasks/` | 任务；019 已 cancelled |
| `AGENTS.md` | Agent 规则 |
| `docs/domain/protocol-draft.md` | 协议 |

## 本地

`.agent/local-environment.md`（gitignore）。日常源码桌面运行：`cd ui && npm run desktop:standalone`。

Linux/Windows 设置页均可开启「登录时自动启动」。Linux 卸载前应先关闭开关；Windows NSIS 卸载会清理当前用户 Run 值。开发壳依赖 Vite，不允许作为自启目标。
