# task-019 · （已取消）收图落盘 + 文件剪贴板捷径

## 状态

`cancelled`

## 原目标

收到图片后落盘并写入 OS 文件剪贴板，使桌面可粘贴出 png。

## 取消原因

- 用户明确：**方案 A 不做**  
- 与正式 **V2 文件通道**（元数据 + 按需传输 + 进度）方向重复，且产品语义含糊  

## 替代

见 `docs/plans/current.md`：**V2 文件通道** 为下一开发主线；新会话「开始开发」时另建文件协议/传输子 task，**不要**复活本 task。
